{%- style -%}
  .snoozepod-product-details-{{ section.id }} {
    max-width: 1360px;
    margin: 16px auto 28px;
    padding: 0 16px;
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__card {
    padding: 22px;
    border-radius: 22px;
    border: 1px solid rgba(7, 25, 66, 0.10);
    background: #ffffff;
    box-shadow: 0 20px 50px rgba(7, 25, 66, 0.05);
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__heading {
    margin: 0;
    color: #071942;
    font-size: 26px;
    line-height: 1.08;
    letter-spacing: -0.03em;
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__body {
    margin: 12px 0 0;
    color: rgba(7, 25, 66, 0.76);
    font-size: 15px;
    line-height: 1.5;
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
    margin-top: 20px;
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__item {
    height: 100%;
    padding: 18px;
    border-radius: 18px;
    background: #f5f7fb;
    border: 1px solid rgba(7, 25, 66, 0.08);
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__item-title {
    margin: 0;
    color: #071942;
    font-size: 18px;
    line-height: 1.2;
    font-weight: 700;
  }

  .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__item-text {
    margin: 8px 0 0;
    color: rgba(7, 25, 66, 0.78);
    font-size: 15px;
    line-height: 1.5;
  }

  @media screen and (min-width: 990px) {
    .snoozepod-product-details-{{ section.id }} {
      margin: 28px auto 32px;
      padding: 0 32px;
    }

    .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__card {
      padding: 30px;
      border-radius: 28px;
    }

    .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__heading {
      font-size: 32px;
    }

    .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__body {
      font-size: 17px;
      max-width: 760px;
    }

    .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 18px;
    }

    .snoozepod-product-details-{{ section.id }} .snoozepod-product-details__item {
      padding: 22px;
    }
  }
{%- endstyle -%}

<section class="section">
  <div class="snoozepod-product-details-{{ section.id }}">
    <div class="snoozepod-product-details__card">
      {%- if section.settings.heading != blank -%}
        <h2 class="snoozepod-product-details__heading">{{ section.settings.heading | escape }}</h2>
      {%- endif -%}
      {%- if section.settings.body != blank -%}
        <p class="snoozepod-product-details__body">{{ section.settings.body | escape }}</p>
      {%- endif -%}

      {%- if section.blocks.size > 0 -%}
        <div class="snoozepod-product-details__grid">
          {%- for block in section.blocks -%}
            <div class="snoozepod-product-details__item" {{ block.shopify_attributes }}>
              <h3 class="snoozepod-product-details__item-title">{{ block.settings.heading | escape }}</h3>
              <p class="snoozepod-product-details__item-text">{{ block.settings.text | escape }}</p>
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Product details",
  "class": "section",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "What to compare"
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "Body",
      "default": "Review the feel, support, materials, and fit that shape how this product performs."
    }
  ],
  "blocks": [
    {
      "type": "detail_item",
      "name": "Detail item",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Comfort"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Compare cushioning, pressure relief, and overall feel based on how you like the surface to respond."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product details",
      "blocks": [
        {
          "type": "detail_item",
          "settings": {
            "heading": "Comfort",
            "text": "Compare cushioning, pressure relief, and overall feel based on how you like the surface to respond."
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "heading": "Support",
            "text": "Focus on alignment, stability, and how well the build supports the areas that carry the most pressure."
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "heading": "Materials",
            "text": "Construction details help explain airflow, durability, responsiveness, and the feel you notice first."
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "heading": "Best For",
            "text": "Use this to compare sleep positions, comfort preferences, and the kinds of needs this product may suit best."
          }
        }
      ]
    }
  ]
}
{% endschema %}
